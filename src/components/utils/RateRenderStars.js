import React from 'react';
import {useUser} from "./userProvider/UserProvider";

const RateRenderStars = () => {

    const user = useUser();

    return (
        <div>
            
        </div>
    );
};

export default RateRenderStars;